# ============================
# Core web framework (Python 3.8+; tested on 3.13)
# ============================
fastapi>=0.126.0
uvicorn[standard]>=0.40.0

# ============================
# API clients & utilities (Python 3.8+)
# ============================
openai>=2.21.0
httpx>=0.28.1
requests>=2.32.3
python-dotenv>=1.0.1
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ============================
# File handling & uploads (Python 3.8+; Pillow needs 3.9+)
# ============================
python-multipart>=0.0.20
aiofiles>=24.1.0
Pillow>=11.1.0

# ============================
# Document parsing (MinerU)
# MinerU 2.7.6 supports Python 3.10-3.13 on Linux/macOS, and up to 3.12 on Windows (Feb 2026).
# ============================
mineru[all]>=2.7.6

# ============================
# OCR for handwritten formulas (Python 3.9-3.13 recommended)
# Pin onnxruntime to a cp310-compatible build; bump if you move to Python 3.11+
# ============================
pix2tex>=0.1.2
onnxruntime==1.23.1
torch>=2.6.0
torchvision>=0.21.0
transformers>=4.48.0
datasets>=3.2.0

# ============================
# Math knowledge & computation (Python 3.8+)
# ============================
sympy>=1.13.3
wolframalpha>=5.1.3
latex2sympy2>=1.9.1

# ============================
# Numerical / scientific computing
# numpy    — ndarray, linear algebra, FFT, random
# scipy    — optimisation, integration, interpolation, sparse, signal, stats
# mpmath   — arbitrary-precision arithmetic
# matplotlib — plotting (server-side PNG/SVG generation)
# pandas   — tabular data wrangling
# networkx — graph/combinatorics algorithms
# scikit-learn — ML utilities (PCA, regression, clustering)
# numba    — JIT-compiled numerical kernels
# cvxpy    — convex optimisation modelling
# ============================
numpy>=1.26.0
scipy>=1.14.0
mpmath>=1.3.0
matplotlib>=3.9.0
pandas>=2.2.0
networkx>=3.3
scikit-learn>=1.5.0
numba>=0.60.0
cvxpy>=1.6.0

# ============================
# Export functionality (Python 3.7+)
# ============================
pypandoc>=1.15

# ============================
# Testing & development (Python 3.8+)
# ============================
pytest>=8.3.4
pytest-asyncio>=0.25.3